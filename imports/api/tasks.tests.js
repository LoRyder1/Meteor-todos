// eslint-env mocha

import { Meteor } from 'meteor/meteor';

if (Meteor.isServer) {
  describe('Tasks', () => {
    desribe('methods', () => {
      it('can delete owned task', () => {
      });
    });
  });
}